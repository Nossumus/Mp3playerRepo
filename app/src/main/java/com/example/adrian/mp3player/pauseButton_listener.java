/*package com.example.adrian.mp3player;


import android.app.NotificationManager;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.widget.Toast;

/**
 * Created by Adrian on 30.04.2018.
 */

/**
public class pauseButton_listener extends BroadcastReceiver {



    @Override
    public void onReceive(Context context, Intent intent) {
       //NotificationManager manager = (NotificationManager) context.getSystemService(Context.NOTIFICATION_SERVICE);
       //manager.cancel(intent.getExtras().getInt("id"));
        Toast.makeText(context, "Button clicked", Toast.LENGTH_LONG).show();
       // mainActivity.pausePlayAction();
        //mainActivity.isThereNotificationSet=true;
       // mainActivity.isThereLocationSet=true;
       // mainActivity.pausePlayAction();


    }
    //Intent button_intent = new Intent("PlayPause");




}
*/